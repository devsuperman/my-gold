<?xml version="1.0" encoding="utf-8" ?>
<toolkit:Popup 
            xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
            xmlns:sf="clr-namespace:Syncfusion.Maui.Toolkit.TextInputLayout;assembly=Syncfusion.Maui.Toolkit"
            x:Class="MyGold5.Pages.Popups.FilterByCategoryPopup"
            xmlns:pagemodels="clr-namespace:MyGold5.PageModels"
            xmlns:models="clr-namespace:MyGold5.Models"
            BackgroundColor="{AppThemeBinding Light={StaticResource LightSecondaryBackground}, Dark={StaticResource DarkSecondaryBackground}}"
            x:DataType="pagemodels:FilterByCategoryPopupPageModel"
            x:Name="Popup">

    <VerticalStackLayout>

        <CollectionView
            ItemsSource="{Binding Categories}">

            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="20"/>
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:Category">

                    <VerticalStackLayout Spacing="20">

                        <Label
                        HorizontalTextAlignment="Center"
                        Style="{StaticResource SubHeadline}"
                        Text="{Binding Name}">

                            <Label.GestureRecognizers>
                                <TapGestureRecognizer 
                                Command="{Binding Source={x:Reference Popup}, Path=BindingContext.NewSelectedCategoryCommand}"
                                CommandParameter="{Binding .}"/>
                            </Label.GestureRecognizers>

                        </Label>

                        <BoxView Style="{StaticResource Divider}"/>
                        
                    </VerticalStackLayout>

                </DataTemplate>

            </CollectionView.ItemTemplate>

        </CollectionView>

        <!--<sf:SfTextInputLayout
             Hint="Category"
             SemanticProperties.Description="ComboBox to Select Category"
             SemanticProperties.Hint="State Collapsed">

            <Picker 
                SemanticProperties.Hint="Select the category for the expenses"
                ItemsSource="{Binding Categories}"
                SelectedItem="{Binding SelectedCategory}"
                ItemDisplayBinding="{Binding Name}"
                SemanticProperties.Description="Category State Expanded"
                x:Name="CategoryPicker">

                <Picker.Behaviors>
                    <toolkit:EventToCommandBehavior
                        EventName="SelectedIndexChanged"
                        Command="{Binding Source={x:Reference Popup}, Path=BindingContext.NewSelectedCategoryCommand}"
                        CommandParameter="{Binding SelectedCategory}"/>
                </Picker.Behaviors>

            </Picker>

        </sf:SfTextInputLayout>-->

    </VerticalStackLayout>

</toolkit:Popup >