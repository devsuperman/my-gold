<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:MyGold5.PageModels"
             xmlns:models="clr-namespace:MyGold5.Models"
             xmlns:controls="clr-namespace:MyGold5.Pages.Controls"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:sf="clr-namespace:Syncfusion.Maui.Toolkit.TextInputLayout;assembly=Syncfusion.Maui.Toolkit"
             x:Class="MyGold5.Pages.SpentListPage"
             x:Name="Page"
             x:DataType="pageModels:SpentListPageModel"
             Title="Expenses">

    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior
                EventName="Appearing"                
                BindingContext="{Binding Path=BindingContext, Source={x:Reference Page}, x:DataType=ContentPage}"
                Command="{Binding AppearingCommand}" />
    </ContentPage.Behaviors>

    <Grid Padding="20">

        <Grid RowDefinitions="Auto,Auto,*">

            <Grid ColumnDefinitions="Auto,*,Auto">

                <Button
                        Grid.Column="0"
                        ImageSource="{StaticResource IconPrevious}"
                        Command="{Binding PreviousMonthCommand}">
                </Button>

                <Label 
                        Grid.Column="1"
                        HorizontalTextAlignment="Center"
                        Style="{StaticResource Title1}"
                        Text="{Binding Title}"/>

                <Button
                        Grid.Column="2"
                        ImageSource="{StaticResource IconNext}"
                        Command="{Binding NextMonthCommand}"/>

            </Grid>

            <sf:SfTextInputLayout
                    Grid.Row="1"
                     Hint="Category"
                     SemanticProperties.Description="ComboBox to Select Category"
                     SemanticProperties.Hint="State Collapsed">

                <Picker 
                        SemanticProperties.Hint="Select the category for the expenses"
                        ItemsSource="{Binding Categories}"
                        SelectedItem="{Binding SelectedCategory}"
                        ItemDisplayBinding="{Binding Name}"
                        SemanticProperties.Description="Category State Expanded"
                        x:Name="CategoryPicker">

                    <Picker.Behaviors>
                        <toolkit:EventToCommandBehavior
                                EventName="SelectedIndexChanged"
                                BindingContext="{Binding Path=BindingContext, Source={x:Reference CategoryPicker}, x:DataType=Picker}"
                                Command="{Binding Source={x:Reference Page}, Path=BindingContext.LoadExpensesCommand, x:DataType=ContentPage}" />
                    </Picker.Behaviors>

                </Picker>

            </sf:SfTextInputLayout>

            <CollectionView 
                Grid.Row="2"
                ItemsSource="{Binding ListGroup}"
                IsGrouped="True"
                SelectionMode="Single"
                SelectedItem="{Binding SelectedItem}"
                SelectionChangedCommand="{Binding OpenItemCommand, Source={RelativeSource AncestorType={x:Type pageModels:SpentListPageModel}}, x:DataType=pageModels:SpentListPageModel}"
                SelectionChangedCommandParameter="{Binding SelectedItem}">

                <CollectionView.GroupHeaderTemplate>
                    <DataTemplate x:DataType="models:SpentGroup">

                        <StackLayout>
                            <Label
                                HorizontalTextAlignment="Center"
                                Grid.Row="0"
                                Style="{StaticResource Body2}"
                                Margin="20"
                                Text="{Binding Name}"/>
                        </StackLayout>

                    </DataTemplate>
                </CollectionView.GroupHeaderTemplate>

                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="5"/>
                </CollectionView.ItemsLayout>
                
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Spent">

                        <Border 
                            Background="{AppThemeBinding Light={StaticResource LightSecondaryBackground},Dark={StaticResource DarkSecondaryBackground}}"
                            SemanticProperties.Description="{Binding Name, StringFormat='{0}'}">

                            <Grid ColumnDefinitions="*,Auto">

                                <Label 
                                    Grid.Column="0"
                                    Text="{Binding Name}"/>

                                <Label 
                                    Grid.Column="1"
                                    Text="{Binding Value,StringFormat='{0:C}'}"/>
                            </Grid>

                        </Border>

                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>


        <controls:AddButton
            Command="{Binding AddItemCommand}"
            SemanticProperties.Description="Add" 
            ToolTipProperties.Text="Add" />
        
    </Grid>
</ContentPage>